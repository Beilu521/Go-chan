APP_ENV=dev
APP_PORT=8080

DATABASE_DSN=host=localhost user=lingyu password=lingyu_dev_password dbname=lingyu_ai port=5432 sslmode=disable TimeZone=Asia/Shanghai

JWT_SECRET=change_me_to_a_long_random_secret
JWT_EXPIRE_HOURS=720

# 必须是 32 字节。MVP 本地先用这个，生产环境必须换成随机密钥。
APP_ENCRYPTION_KEY=12345678901234567890123456789012

# 第一版可以先用 mock。以后改成 http 调 Hermes。
AGENT_MODE=mock
HERMES_BASE_URL=http://localhost:9000